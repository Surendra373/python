#creating a list and removing the items from the lists
my_lists = [45,56,24,78,10,34]

# removing the item from the list
my_lists.remove(45) #as a example we are removing no 45
print(my_lists)

#calculate the maximum andminimum value from the lists
max_value = max(my_lists)
min_value = min(my_lists)
print("maximum value:",max_value)
print("minimum value",min_value)